// Empty stub for side-effect-only imports of WordPress-provided packages that
// aren't installed as real dependencies (they exist as `wp.*` globals at
// runtime). Importing them in tests only needs to not throw.
module.exports = {};
